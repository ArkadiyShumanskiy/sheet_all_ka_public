import { styled } from "@mui/material/styles";
import { Typography } from "@mui/material";

export const TypographyHeadlines = styled(Typography)(({ theme }) => ({
  fontFamily: "Roboto",
  fontWeight: "500",
}));
